'use strict'

const {
  OPERATION_KINDS,
  OPERATION_OWNERS,
  healthOperation,
  operationByAction,
  operationCatalog,
} = require('./operation-registry')

module.exports = {
  OPERATION_KINDS,
  OPERATION_OWNERS,
  healthOperation,
  operationByAction,
  operationCatalog,
}

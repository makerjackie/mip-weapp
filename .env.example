# HTTP adapter for the shared mip-admin-api. Leave empty for local demo mode.
VITE_MIP_ADMIN_API_URL=
# Optional one-time-code exchange endpoint, defaults to <API_URL>/auth/session.
VITE_MIP_ADMIN_AUTH_URL=
# Bearer token only; do not commit a real token.
VITE_MIP_ADMIN_TOKEN=

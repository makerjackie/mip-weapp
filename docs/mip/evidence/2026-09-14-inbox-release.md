# 2026-09-14 消息分页与已读修复发布

## 发布范围

- 代码提交：`d7d21b8c62a4b5b9591e369a1040f6a38d6195de`，已推送 `origin/main`。
- 从该提交的独立、干净快照执行构建与上传，未包含工作区中并行修改的访客页面和个人中心视图。
- CloudBase 目标为本机已配置的 staging 环境，只更新 `mip-notifications-api`。
- 小程序开发版本：`0.2.0.20260914.1`。
- 更新说明：修复消息首屏与未读角标，支持触底加载和全部已读（d7d21b8）。
- 未执行微信提审、正式发布或体验版切换；没有数据库迁移。

## 检查

独立发布快照的完整 `pnpm verify:all` 退出码为 0：小程序前端 224 个测试文件、1204 项测试通过，服务端 263 个测试文件门禁通过；Web 的 Node 测试、11 个前端测试文件、56 项前端测试、构建及响应式合同通过。消息回归覆盖默认 20 条、触底分页、非空服务端 DTO 到页面展示、跨页已读同步、全部已读及失败恢复、旧未读响应覆盖保护。

复用本机已安装的相同锁文件依赖时，仅在发布进程设置 `pnpm_config_verify_deps_before_run=false`，避免 pnpm 在依赖软链接目录中自动重新安装；所有项目质量门禁照常运行。生成配置来自本机，源码与 Git 提交保持一致。

## 云端证据与限制

API Key 通道成功更新函数代码，并完成配置及代码变更回读，随后 `manageFunctions.invokeFunction` 返回 `Cam authentication failed`。因此原部署命令退出码为 1，不能把整个部署脚本记为通过；全量 `cloud:verify` 同样因 `mip-identity-api` 缺少有效调用证据失败，不能宣称所有云函数均完成验收。没有发起新的 Device Flow 授权，也没有放宽调用权限。

针对本次函数另外完成以下验证：

- SCF 回读为 `Active` / `Available`、`Nodejs20.19`，修改时间为 `2026-09-14 21:27:12`（云端返回值）。
- 函数调用规则为已登录且非匿名；完整 trigger inventory 证明没有 timer。
- 通过已登录微信开发者工具的小程序实际上下文调用同一环境，AppID 一致性检查通过。`health` 返回 `ok=true`、`persistence=cloudbase-mysql`。
- 实际 `listInbox` 请求上限为 20，当前测试账号返回 7 条消息、未读数 0、当前页未读数 0、无下一页。
- 本次只读验收未替用户执行全部已读。批量操作的真机交互效果仍待验收。

以上证明本次消息函数已更新且小程序调用正常，不替代全量云端管理验收或其他业务的真机验收。

## 微信上传证据

在完整门禁通过后，通过已有微信开发者工具登录态执行 `weapp upload`，退出码为 0，CLI 返回 `✔ upload`。上传包总大小为 3,428,432 字节，主包 1,869,505 字节。上传来自上述独立代码快照；没有读取或复制上传私钥。

日志保留在本机临时目录，公开记录不包含 AppID、EnvID、用户标识、凭据或消息正文。

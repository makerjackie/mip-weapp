# 会员内容后台配置验收（2026-09-26）

## 最新决定与入口

用户确认采用后台配置，允许先使用演示内容，不再等待最终资料才开发。

- Web「成长与勋章 → 会员内容配置」：等级门槛、关联权益、权益说明、现有成长事件奖励与每日上限、勋章定义、会员服务协议。
- Web「成长与勋章 → 运营操作」：沿用勋章发放/撤销和成长调整，保留授权、原因与流水。
- Web「任务管理」：沿用任务奖励配置及完成审核发放，修改配置不追溯改写已发放记录。
- 小程序「账号设置 → 会员服务协议」：读取后台保存的纯文本、版本及演示标记，提供加载、空内容、失败重试。

「填写演示示例」只预填表单，等级/权益/勋章默认为草稿；管理员可修改并启用。协议有可编辑演示正文和独立演示标记。此轮未向真实用户自动发放测试奖励；授权恢复后只保存演示协议和一项权益草稿，见下方真实联调记录。

## 本地验证

- 新增协议权限、版本冲突、应用隔离、字段白名单、长度上限与非空响应测试；BFF 白名单新增协议和四项配置保存能力。
- 新增 Web 合同及 React 交互测试，覆盖保存的版本/幂等键、只读权限和读取失败时不回退演示正文。
- 修复等级清空权益后旧 JSON 内容继续显示的问题，增加回归断言。
- 浏览器使用明确标记的本地模拟接口，实际操作协议编辑、保存、刷新回读；检查勋章与等级演示表单。
- 390×844、1280×720、1440×900 检查无页面横向溢出；前者协议正文、后两者配置弹窗可用。
- 小程序运行预检通过：开发者工具已登录，服务端口可用；未将预检记为新协议云端联调通过。

完整 `pnpm verify:all` 通过：小程序 245 文件 / 1336 测试；服务端 659 源文件 / 279 测试文件；Web 149 合同测试、66 React 测试，类型、lint、构建和响应式合同通过。`git diff --check` 通过。

## 发布与真实联调（授权恢复后）

- 维护者完成 Device Flow 授权，MCP 授权状态为 READY。以 `CLOUDBASE_AUTH_MODE=local` 更新 outbox、identity、tasks、opportunities、admin 五个核心函数，逐个配置/代码回读与 MySQL 健康检查通过。
- 完整 `cloud:verify` 通过：schema、最小权限、核心函数健康与受保护调用规则均通过；未创建高频 MySQL 定时器。
- Web 正式分支 main 部署完成，最终部署标识 `aad79fa2`，域名 https://mipmini.01mvp.com 。首次部署旧资源路径在当前浏览器出现空白；以 `vite build --assetsDir assets/release-f78c3a3b` 重新构建并部署后，正式域名真实会话和新版界面正常。此次只更换构建资源路径，业务源码仍为 `f78c3a3b`。
- 小程序开发版 `0.2.0.20260926.1` 在重新通过完整 `verify:all` 后上传成功，返回 4 项分包信息。上传日志有 miniprogram-ci 子进程 `hasFlag` 兼容告警，但最终返回成功和退出码 0；不把上传等同体验版切换、提审或正式发布。
- 实际通过 Web 保存演示协议，数据库限定当前应用回读：版本 1、isDemo=true、正文 248 字符；开发者工具新构建实拍展示相同正文、版本与演示标识，完成后台到小程序读取联调。
- 实际通过 Web 新建“演示·社区交流资料”，回读显示草稿与说明，可后续修改/启用。未给真实用户发放测试权益或勋章。
- 人工点击小程序任务“已结束”，显示“暂无已结束任务”，不再错误显示待完成任务。

本轮未重新完成全路由、全部严格交互和非空导出验收。既有后台导出下载失败、全路由未通过项仍见[前轮收尾记录](../closeout-20260926/README.md)，不因部署成功自动标记完成。手机号、支付、扫码及通知送达仍需真机验证。

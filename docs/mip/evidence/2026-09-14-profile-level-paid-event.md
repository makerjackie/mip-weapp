# 玩家等级卡片与付费测试活动

- 用户真机确认会员支付已正常，随后反馈“我的”玩家等级卡片文字重叠。
- 根因：`level-banner-deco@3x.png` 内含固定示例等级、经验值和进度条，组件又叠加真实数值。组件现只显示图片右半部分的装饰，保留独立的真实等级、经验和进度条；右侧入口改为相对右边距布局。
- 在开发者工具 375px 页面截图中观察到单一“起步”和“50 / 500”，没有固定示例文字重叠。此为开发者工具视觉证据，不替代手机验收。
- 使用已登录 Web 后台建立“ MIP 活动支付测试（0.10元）”测试活动，20 个名额、自动确认报名、价格 10 分；说明明确无线下聚会。活动时间为 2026-09-21 20:00–21:00，报名截止当日 19:00（北京时间）。
- 创建时发现 `mip_events` INSERT 的 33 列对应了 34 个值。补充真实 repository SQL 形状测试后复现失败，移除多余占位符后 9 项 repository 测试通过，部署 `mip-admin-api` 后通过正常后台成功建立草稿。没有直接写数据库业务记录。
- 草稿的内容检查返回 ERROR：Web BFF 调用不具备小程序云调用令牌。内容检查增加服务端微信 API 适配器，使用已有服务端配置、缓存访问令牌、最多刷新重试一次，网络与提供商异常失败关闭且不泄漏凭证。继续保留内容审核与发布门槛；内容、openid 和版本/场景字段仍由原有完整分段检查流程提交。
- 内容检查、提供商错误/敏感内容/过期令牌以及 repository 专项共 15 项通过，修复已部署。草稿 ID 为 `bff2b567-1173-4cf8-a97a-9897610e27cc`；等待重新保存完成内容检查并发布。Chrome 控制连接中断，已请用户将后台切回前台。
- 原 399 元活动仍为 UNPUBLISHED，历史订单金额未更改。
- 最终 `VITEST_MAX_WORKERS=1 pnpm verify:all` 退出 0；小程序/服务端与 Web 全量检查、构建、响应式合同通过。`runtime:preflight` 与部署后 CloudBase schema/权限/健康检查通过。
- Chrome 原生操作返回用户已切换应用的中断，扩展连接也不可用；最终仍未执行重新保存与发布，不声明付费活动可报名。已请求用户恢复后台前台窗口。

## 成长任务文案与发布重试

- 成长页任务摘要采用单行弹性布局：任务名、经验奖励、动作分别布局，移除第二行说明；“完善合作资料”显示为“完善资料”，seed 名称同步更新。未改奖励金额、领取条件或任务状态。
- 用户再次发布失败后，回读测试活动仍为 DRAFT / ERROR / version 1，即初次创建时的检查异常尚未刷新。发布操作现在针对 ERROR 自动读取当前版本内容并重试真实检查；只有 PASSED 才把同版本的检查结果与发布状态一起提交。实际违规、检查服务异常或版本冲突继续阻止发布，不直接将异常标记为通过。
- 服务与 repository 26 项专项通过，覆盖自动重试通过、内容拒绝、服务异常、旧版本、缺失检查凭据等路径；`mip-admin-api` 修复已部署。已请用户直接重试发布，尚未据此声明活动已发布。
- 本轮最终 `VITEST_MAX_WORKERS=1 pnpm verify:all` 退出 0，`runtime:preflight` 和部署后的 CloudBase 总验收通过。新版成长页截图因开发者工具连接超时未完成，不能声明真机视觉通过。最新测试活动回读仍为 DRAFT / ERROR / version 1，待用户执行修复后的发布重试。

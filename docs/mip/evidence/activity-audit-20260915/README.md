# 活动流程审计与回归（2026-09-15）

## 范围与基线

开始时工作区干净，基线为 `51fb662`。Luna 分别执行现场页面、Web 和活动服务端任务；主审核复核代码、重写不足的测试，并运行开发者工具检查。没有把旧问题清单当成已确认缺陷。

## 修复

- 现场入口删除重复说明；四个现场页面左右边距由 48rpx 调整为 24rpx。海报未生成时不显示无用的保存按钮。
- 活动现场页忽略页面隐藏后或后续刷新之前发出的旧请求结果。
- 报名页在等待邀请信息前锁定提交，防止连续点击产生重复请求；不确定请求的重试保留原幂等键。
- 修改活动标题等普通内容不再递增报名表版本；结构实际变化才递增，忽略对象键顺序差异。
- 海报码兼容 PNG/JPEG 和有偏移的二进制视图；扩展名、媒体类型与实际格式一致，保留对象绑定和失败清理。
- Web 活动操作缺少服务端版本时拒绝提交；创建活动的必填校验不再产生未处理 Promise 异常。
- Web DOM 测试并发限制为 2，减少同时启动多个 jsdom/Ant Design 页面造成的 CPU 争用；保留原超时与断言。

## 测试方式

- `cloudfunctions/mip-admin-api/tests/event-lifecycle.integration.test.js`：同一内存数据夹具调用真实 repository/service，依次创建、发布、报名、读取非空名单、签到、重复签到、撤销、结束和查询本人报名。模拟数据库不是实际 MySQL，权限与 SQL 并发另由既有专项测试覆盖。
- `tests/admin-onsite-workbench.test.ts`：真实 DTO parser、Page 方法、延迟响应、海报绘制和失败重试；Canvas、云请求是测试替身。
- `tests/mip-registration-page-recovery.test.ts`：必填、提交锁、请求失败重试及冲突后的草稿恢复。
- `admin-web/src/features/admin-runtime/event-form-dialog.react.test.tsx`：真实 Ant Design 表单的必填失败、内容填写、日期转换及发布版本传递；不是生产浏览器端到端验收。

## 开发者工具结果

本机登录和服务端口预检通过。实测窗口为 375×724。通过实际页面方法，从授权列表打开活动现场，再进入参与者名单，读到 3 场可用活动及选中活动的 4 条 `REGISTERED` 报名。截图确认新页面 24rpx 左右边距。没有修改参与者的实际报名或签到状态。

修复前，在现有 staging 环境的未来演示活动上分别生成静态海报和短时码，两者均返回 `SERVICE_UNAVAILABLE`。回读确认请求写入了签到凭证，但未完成图片生成；未把这个结果写成海报通过。旧日志查询接口已下线，CLS 查询返回 `topic not exist`，因此不能仅凭本地兼容修复断言现网失败的唯一根因。

完整路由运行时检查未通过：发现页、活动页等渐进媒体数据仍包含 `cloud://`，触发当前运行时数据检查，随后身份测试夹具恢复失败。专项活动页面通过不替代完整路由报告。

## 外部验收边界

真实微信扫码、重复扫码、撤销后的重新扫码、相册保存、手机号、支付回调与退款仍需真机验收。当前报名写入自动化仅允许 development/test，现有环境为 staging，未通过修改环境标签绕过限制。

## 最终本地验证

最终 `pnpm verify:all` 完整执行至 Web 响应式检查通过：小程序 225 个测试文件、1,215 项测试；服务端合同检查通过（614 个源码、266 个测试文件）；Web 合同测试 117 项、React 测试 12 个文件共 60 项全部通过；两端构建及预算/响应式检查通过。`pnpm docs:check`、`git diff --cached --check` 通过。

此前默认 Web 并发的复跑出现 4 条页面测试超时；限制为 2 个 worker 后，本轮 60 项通过，未延长超时或删除断言。这是本地测试结果；授权后的云端海报码验收见下节，真机全链路仍未完成。

## 授权后的部署与海报修复

维护者明确授权后，复用了本机 Device Flow。`mip-admin-api` 与 `mip-events-api` 均通过仓库脚本完成部署和健康检查；使用相同认证模式的 `cloud:verify` 通过全部核心函数、MySQL、最小权限和调用规则检查。

安全阶段诊断确认，旧海报链路在 `wxacode.getUnlimited` 抛出 `-604101`，尚未进入存储。活动码现采用与后台登录码一致的官方微信 HTTPS 路径，服务端严格绑定 `MIP_WECHAT_APP_ID` / `MIP_WECHAT_APP_SECRET`。部署脚本注入配置并开启所需公网访问，云端验收回读校验；没有放宽客户端调用权限。未配置 HTTP 凭证时保留云调用；配置不全或 AppID 不匹配时拒绝调用。

补测覆盖令牌缓存、失效时仅刷新一次、其他错误不重试、响应流大小上限、网络及响应体错误脱敏。令牌只保留在进程内存。失败响应只显示固定阶段文案与数字错误码，不返回原始微信错误、URL、用户身份或凭据。

实际开发者工具复测：静态海报、短时码均生成成功；导出图片为 1125×1680，人工查看确认标题、时间、城市、微信码及说明完整；页面图片加载后显示完整。短时码倒计时从 05:51 递减至 05:50，未过期；随后参与者名单仍正常显示 4 条报名。本次没有执行真实参与者的签到、撤销或报名变更。

这些结果覆盖海报生成、文件导出、页面显示和倒计时；不替代真机扫描、相册权限和完整真实用户生命周期验收。运行时图片包含演示活动签到码，只保留本地临时证据，未加入仓库。

## 界面截图

[活动现场](events.png) · [活动控制台](event-console.png)。来自上述 staging 演示活动、375px 开发者工具窗口；不包含参与者名单或可用签到码。

# 活动实际操作验收（2026-09-15）

基线 `6634826`。本轮使用真实 Web 后台、已登录微信开发者工具与 staging/TEST 环境。测试活动名为“验收测试-20260915-活动全流程”，免费、自动确认、名额 5；仅允许当前测试账号参与。不修改已有活动的报名记录，不用 SQL 构造验收结果。

## 已完成的实际操作

| 项目 | 结果与证据 |
| --- | --- |
| Web 登录 | 小程序现场工作台确认登录码后，Web 显示已验证会话、真实数据 |
| 空表单 | 名称、摘要、介绍、开始与结束时间缺失时拦截提交 |
| 创建 | 从 Web 新建，列表新增草稿；详情时间、地点、名额与输入一致，版本 1 |
| 发布 | Web 发布后列表显示已发布；首次操作发现弹窗遮挡，关闭详情侧栏后完成发布 |
| 用户端可见 | 小程序活动列表出现同名活动，详情显示“立即报名” |
| 报名页 | 从详情进入报名页，显示免费、当前账号资料、取消截止时间；原后台未配置额外报名问题 |
| 新活动海报 | 静态海报、短时码生成成功；图片加载后确认标题、时间、城市、微信码完整 |
| 短时码过期 | 观察约 6 分钟自然倒计时直至“已失效”；刷新后恢复 05:50，失效标记清除 |
| 下架与重新发布 | 下架后小程序下拉刷新列表不再显示该活动；重新发布成功 |
| 报名问题跨端同步 | 网页新增“你希望交流什么”必填题，保存后重新打开编辑器回读一致；重新发布后小程序显示同名必填题，空值提示必填、填写后校验错误清除 |

小程序证据来自实际 Page 方法与真实云服务、页面数据和开发者工具截图；不是手机扫码，也不是所有操作都通过物理点击执行。截图与临时码只保存在本地 `.tmp/activity-tester/`，不提交有效签到码或账号资料。

## 实际发现并修复

1. **Web 详情侧栏遮挡发布操作窗口**：真实截图和无法触发的按钮确认。统一详情、操作表单和确认窗口层级；在浏览器中保持侧栏打开，两个确认按钮都能响应。
2. **手机日期时间选择器溢出**：390px 屏幕上原页面横向宽度达到 481px，时间列被裁掉。日期与时间改为纵排，面板限制在可视区域内并允许纵向滚动；实际选中日期并确认回填成功。
3. **Web 缺少报名字段配置**：新建/编辑只有隐藏的 schema，运营无法配置问题。增加文字、长文字、单选、勾选和必填配置，自动生成稳定字段标识；对齐服务端 12 字段、字段名称 60 字、选项与字数限制。直接调用真实服务端 schema validator 的合同测试防止前后端漂移。

主审核发现并改正执行稿中的字段标识格式、长度限制和日期面板选择器问题，没有把执行稿的本地测试结果当成验收结论。

## 本地验证

- `pnpm verify:all` 退出码 0：包括小程序、服务端、Web 的检查、测试和构建；Web 合同测试 118 项，React 测试 62 项通过。
- 后端活动专项 239 项通过；生命周期内存数据库覆盖创建、发布、报名、非空名单、签到、重复签到、撤销、结束。此项不算真实账号写入验收。
- 浏览器使用产品真实组件进行本地可视化复测，覆盖 1280×720、1440×900、390×844；演示组件场景不连接服务端。检查弹窗、日期选择和报名字段连续输入，未见横向溢出。
- `git diff --check` 通过。

## 部署复测

`pnpm --dir admin-web deploy:pages` 完成，部署标识 `ea1d0619`。在实际自定义域名刷新后，保持详情侧栏打开，完成下架、编辑报名问题、保存和重新发布操作；确认弹窗未再被遮挡。当前测试活动版本 5、已发布，有效报名与签到均为 0；测试报名内容已填写但尚未提交，保留供后续继续验收。本轮未上传新的微信版本。

## 2026-09-15 留待续测（已由下节更新）

报名页明确注明提交即同意服务协议和隐私政策，已发出当前测试账号的提交确认；收到确认前没有提交报名。因此真实报名记录、重复报名、编辑报名、非空名单同步、签到、重复签到、撤销、取消与重新报名、活动结束后历史状态，仍为 **PENDING**。

第二身份的候补/审核场景、真机扫码、相册保存、手机号、订阅消息、支付与退款仍需独立验收。不能据本报告宣称活动所有功能或真机全链路已经通过。


## 2026-09-16 真实链路续测

在用户确认后继续使用同一测试活动、当前测试账号。报名页保留“提交报名表示已阅读并同意《服务协议》和《隐私政策》”，没有增加二次勾选。以下写操作通过实际小程序/网页按钮提交，连接 staging 真实云服务；部分导航、输入与状态读取使用开发者工具自动化接口。不是 SQL 注入结果或内存数据库模拟。

| 实际操作 | 页面与跨端结果 |
| --- | --- |
| 提交报名 | 小程序显示“报名成功”“报名资格已确认”；Web 有效报名 1/5、签到 0，非空名单显示当前测试账号 |
| 现场核销 | 小程序名单点击“确认签到”，状态变为“已签到”，显示签到时间；Web 刷新后签到数 1 |
| 撤销签到 | 在实际确认窗口填写原因并撤销，恢复“已报名”“未签到”和确认签到入口；Web 签到数恢复 0 |
| 重入报名页 | 已有报名进入“修改报名”，不会新增第二条报名；保存后显示“报名已修改” |
| 取消报名 | 详情页确认取消，恢复“立即报名”；Web 有效人数归零，原报名行保留“已取消” |
| 重新报名 | 必填题空值仍被拦截；填入新答案后提交成功，最终回读答案与输入一致，报名版本 6、formVersion 2 |
| 结束活动 | Web 新增“结束活动”入口，实际提交后版本 5→6、状态“已结束”，有效报名保留 1/5，当前签到 0 |
| 小程序结束状态 | 详情页主按钮为禁用的“活动已结束”，保留历史报名；发现已有报名编辑权限仍开启，已补齐服务端状态门禁（复测见下节） |

核销后按钮改成撤销，撤销后恢复核销入口。本轮未模拟第二个并发请求或物理重复扫码，不把按钮状态验证当作重复扫码验收。

### 本轮修复与验证

- 补齐 Web 手动结束入口，仅已发布且具备写权限的活动显示，复用服务端版本和幂等校验；确认文案说明无法重新发布、保留历史记录。
- `pnpm verify:all` 首次完整通过，Web 合同测试 118 项、React 测试 63 项。
- Web 部署标识 `5d85af22`，在真实自定义域名验证新增入口和结束结果。
- 结束后按既有取消截止规则允许退订，管理员保留补签和纠错能力；这两项没有改成全面锁定，避免改变退款及运营策略。

- 真实复查发现提前结束后 `REGISTERED` 仍能编辑：服务端对 `ENDED/CANCELLED` 拒绝编辑，保留下架活动的既有编辑策略；新增资格与实际更新拒绝测试。专项 14 项、关联 38 项通过。
- 390×844 实际截图发现详情标题被操作按钮挤压：移动端头部改为标题和操作分行。主审核对照真实 DOM 修正执行稿的父子结构判断，桌面布局保持不变。
- 追加服务端修复后的 `pnpm verify:all` 再次退出码 0，包含 Web 合同 118 项与 React 63 项。

### 最终部署与回读

- 最终 Web 部署 `cc9b26bc`；390×844 实际截图确认标题完整、按钮独占下一行，随后恢复默认视口。
- `mip-events-api` 更新代码后，通过同一小程序、同一活动重新打开报名页：`state=blocked`、`registration.canEdit=false`、`editing=false`，显示“当前报名状态或活动时间不支持修改”；答案与报名版本仍保留。
- **云管理验收首次未通过（后续已恢复，见下）**：部署脚本在代码更新后的健康调用处退出 1；单独 `cloud:verify` 也未取得健康响应。进一步只读调用定位为 `[Invoke] Cam authentication failed`，本地 Device Flow 与 API Key 通道均相同。没有绕过验收或扩大账号权限。实际小程序业务回读已证明修复生效，但不能据此宣称完整云管理验收通过。
- `pnpm docs:check` 与 `git diff --check` 通过。

### 仍需独立验收

第二身份的候补/审核、真实并发与重复扫码、真机扫码、相册保存、手机号、订阅消息、支付与退款仍未在本轮完成。手动结束已实测，按自然时间结束未等待到实际结束时刻。本轮未上传新的微信版本。


## 2026-09-16 云管理健康检查恢复

针对 `[Invoke] Cam authentication failed`，使用既有、已授权的 `CLOUDBASE_AUTH_MODE=local` 重启 canonical MCP 常驻进程，没有重新扫码、扩大权限或修改函数代码。重启后同一 `mip-events-api` 健康请求返回 `ok=true`、`persistence=cloudbase-mysql`，证据指向常驻进程缓存的授权状态。

随后完整 `cloud:verify` 退出码 0，2026-09-16 11:46（北京时间）完成 19 个函数验收；数据库结构、最小权限、函数健康、受保护调用规则及禁止高频 timer 检查通过。上节云管理验收阻塞已解除；真机与支付等尚未覆盖的业务场景仍按原记录保留。

恢复步骤已写入 [故障排查](../../../TROUBLESHOOTING.md)。

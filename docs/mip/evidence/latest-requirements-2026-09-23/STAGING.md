# 2026-09-23 TEST staging 迁移与云端读回

目标由本机忽略提交的环境配置绑定，部署阶段为 `staging`，支付模式为 `test`，商品目录为 `TEST`。本记录不包含 EnvID、AppID、数据库账号或凭证；不适用于生产环境。

## 数据库

- 迁移前在仓库外完成逻辑备份：126 张表、1553 行，逐表导出前后行数与文件哈希已核对。备份不是事务性快照，尚未做恢复演练。
- 从原有 60 个迁移开始执行。`20260922086000` 的第 3 步建历史表失败后，迁移器停在 `RUNNING`；只读核对确认该表不存在，前两步已应用且只有这一条未决记录。
- 新增锁定迁移 `20260922085500`，为父表的 `(app_id, id)` 增加唯一索引。恢复脚本先核对目标环境、备份、步骤哈希、表与列，再只清除失败的第 3 步记录；未把未执行的 DDL 标成 `APPLIED`。
- 重跑后 92/92 个锁定迁移应用，目标表清单通过；专用运行账号的 146 张表精确权限收敛并回读通过。

## 核心函数

- API Key 完成了首个函数的代码及配置更新，但函数调用返回 `Cam authentication failed`，因此没有把该次尝试记作健康通过。
- 本机 Device Flow 登录态恢复后，按部署清单更新并健康回读 16 个核心函数。整批执行在游戏函数的 SCF TLS 连接中断时停止；只读核对函数仍为 `Active/Available` 且 MySQL 健康后，使用单函数模式完成游戏及后续 7 个函数。
- 独立 `cloud:verify` 通过：schema、最小权限、16 个函数的 MySQL 健康检查及受保护调用规则均通过。

这些证据只证明当前 TEST staging 的数据库与核心函数状态。尚未执行微信体验版或正式版上传、生产部署、真机支付/手机号/扫码，以及备份恢复演练。小程序页面运行时结论另见本目录 [README](README.md)。

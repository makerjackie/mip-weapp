# 2026-09-23 最新需求本地验收

## 来源和代码边界

- 主工程 `origin/main`：`30537eab`；需求及流程仓库 `origin/main`：`cb5956b`。2026-09-23 再次 fetch 后，两份本地 HEAD 均与对应远端一致。
- 本轮实现位于 `codex/mip-latest-prd-20260923` 工作分支。最新设计依据为需求仓库保留的 `role-flows` 和 [线上设计标注](https://airdrop.z-h-ai.com/p/715a5d)，已删除的 `user-flow` 与 `journey-review-pilot` 不参与验收。
- [需求差异](../../REQUIREMENTS_DIFF_20260922.md)逐项记录原有覆盖、修复范围及仍未实现的旧后台名片管理。

## 本地静态与自动化门禁

在 Node `22.23.1`、pnpm `11.14.0` 下，`pnpm verify:all` 通过：小程序 1309/1309 测试、99 视图/16 核心函数/91 锁定迁移的源码合同、650 服务端源文件/275 测试文件/46 种 outbox 事件的服务端合同、Web 144/144 模块测试与 63/63 React 测试，以及两端构建和响应式静态检查。`git diff --check` 与 `pnpm docs:check` 通过。

`pnpm runtime:preflight` 通过：70 条路由完整、本地真实 AppID 配置匹配、开发者工具已登录且服务端口可用。这里只证明前置条件，不证明业务页通过。

## 开发者工具运行时

- Node 22 的一次完整路由尝试覆盖 70/70：69 条进入合同接受态，新增 `mip-profile-interests` 停在 `blocked`（预期 `ready`/`empty`）。6 个代表性状态通过；机会搜索的严格原生输入交互因开发者工具返回的元素不支持 `input()` 而失败，后续交互未执行。编译器报告 0 错误。该次报告包含组件空值与报名页初始化告警，随后已修正对应源码。
- 修正告警及“禁用态”截图目标后，后续尝试已通过 6 个代表性状态并继续跑页面；个人档案页截图失败触发自动恢复，重新连接开发者工具在 60 秒内超时。最新 `.tmp/runtime/report.json` 记录的是这次工具中断，不是 70 路由完成报告。
- [禁用态之前](disabled-before.png)与[禁用态之后](disabled-after.png)为开发者工具实际截图，已滚动至账号注销控件；后图由运行时验证脚本注入 `processing` 状态，不能证明真实注销交易。
- `mip-profile-interests` 的 `blocked` 只证明当前 staging 未返回可接受名单状态；尚无新接口和相关迁移在该环境部署、读回的证据，不能把本地服务端测试当成该页线上通过。

## Web 视觉与外部能力

本地显式演示模式中，网站概览在 1280×720、1440×900、390×844 检查了布局与页面横向溢出；390×844 的活动列表和活动详情错误态也已检查，均未出现页面横向溢出。演示模式不返回服务端活动详情，因此反馈导出按钮的真实数据、权限和下载链路仍需管理 API 环境验收。

数据库迁移、云函数部署、短信/订阅模板、手机号、支付、扫码签到和其他真机能力均无本轮完成证据。未执行微信体验版/正式版上传或生产部署。

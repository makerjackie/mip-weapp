# 2026-09-23 最新需求本地验收

## 来源和代码边界

- 主工程 `origin/main`：`30537eab`；需求及流程仓库 `origin/main`：`cb5956b`。2026-09-23 再次 fetch 后，两份本地 HEAD 均与对应远端一致。
- 本轮实现位于 `codex/mip-latest-prd-20260923` 工作分支；视觉复核修正提交为 `df7532d5`。最新设计依据为需求仓库保留的 `role-flows` 和 [线上设计标注](https://airdrop.z-h-ai.com/p/715a5d)，已删除的 `user-flow` 与 `journey-review-pilot` 不参与验收。
- [需求差异](../../REQUIREMENTS_DIFF_20260922.md)逐项记录原有覆盖、修复范围及仍未实现的旧后台名片管理。

## 本地静态与自动化门禁

在 Node `22.23.1`、pnpm `11.14.0` 下，`pnpm verify:all` 通过：小程序 1309/1309 测试、99 视图/16 核心函数/92 锁定迁移的源码合同、650 服务端源文件/275 测试文件/46 种 outbox 事件的服务端合同、Web 144/144 模块测试与 63/63 React 测试，以及两端构建和响应式静态检查。`git diff --check` 与 `pnpm docs:check` 通过。

`pnpm runtime:preflight` 通过：70 条路由完整、本地真实 AppID 配置匹配、开发者工具已登录且服务端口可用。这里只证明前置条件，不证明业务页通过。

## 开发者工具运行时

- Node 22 的一次完整路由尝试覆盖 70/70：69 条进入合同接受态，新增 `mip-profile-interests` 停在 `blocked`（预期 `ready`/`empty`）。6 个代表性状态通过；机会搜索的严格原生输入交互因开发者工具返回的元素不支持 `input()` 而失败，后续交互未执行。编译器报告 0 错误。该次报告包含组件空值与报名页初始化告警，随后已修正对应源码。
- 修正告警及“禁用态”截图目标后，后续尝试已通过 6 个代表性状态并继续跑页面；个人档案页截图失败触发自动恢复，重新连接开发者工具在 60 秒内超时。该次工具中断不是 70 路由完成报告。
- TEST staging 部署后再次完整构建并尝试两轮运行时自动化；微信开发版本 `0.2.0.20260923.1` 上传后又重试一轮。三次均在首个代表状态 `state-loading-before` 调用 `App.captureScreenshot` 时连续失败，自动重连在 60 秒内超时。最新 `.tmp/runtime-evidence/2026-09-23-post-upload/report.json` 为 `failed`、页面/代表状态/交互均 0 项完成；不能沿用部署前 69/70 的结果声称本轮通过。
- 在已打开的开发者工具模拟器中手动看到了会员页、个人页及机会列表的正式渲染：个人页等级图横幅、统计区、三个内容 tab 和整块“添加”卡出现；当前账号合作卡与超级案例为空，无法检查有内容时的时间轴。机会页展示真实 Banner 与机会卡。该观察不等于自动化截图对比，也没有覆盖 43 个原型步骤。
- 本轮在同一开发者工具模拟器另取得 7 个流程步骤的实际画面，制作了本机单文件原型/实效并排 HTML；其中 J1-06 等级页与 J6-03 相关机会已在视觉修正后重新截图，其余 5 张仍为前次版本。原型 43 步引用 40 个不同场景；实际截图只覆盖已登录账号的少量页面，J6 的三个档案内容页都是空态，不能证明有内容的卡片、时间轴或跨角色一致。J1-06 左右分别为新玩家加入 Lv.2 和付费玩家 Lv.1，不能签同状态视觉一致；原型权益标题写 2 项却画 4 个图标，口径待确认。HTML 含当前账号和 TEST 数据截图，仅保存在本机，未随仓库推送。
- 视觉修正后的 `0.2.0.20260923.2` 已在完整 `pnpm verify:all` 通过后，使用登录的开发者工具执行上传，CLI 返回 `✔ upload`；这只证明开发版代码上传，不代表切换体验版或正式发布。云函数和 React Web 本次没有代码变更，未重新部署。
- [禁用态之前](disabled-before.png)与[禁用态之后](disabled-after.png)为开发者工具实际截图，已滚动至账号注销控件；后图由运行时验证脚本注入 `processing` 状态，不能证明真实注销交易。
- `mip-profile-interests` 的 `blocked` 是部署前的运行时证据。本轮相关迁移与函数已在 TEST staging 部署、云端读回；受上述截图接口故障影响，该页面仍需重新完成小程序运行时和真实账号数据验收，不能把云函数健康检查当成页面通过。

## Web 视觉与外部能力

本地显式演示模式中，网站概览在 1280×720、1440×900、390×844 检查了布局与页面横向溢出；390×844 的活动列表和活动详情错误态也已检查，均未出现页面横向溢出。演示模式不返回服务端活动详情，因此反馈导出按钮的真实数据、权限和下载链路仍需管理 API 环境验收。

TEST staging 的[数据库迁移、16 个核心函数重新部署、独立云验收及微信开发版/Web Pages 发布读回](STAGING.md)已完成；短信/订阅模板、手机号、支付、扫码签到和其他真机能力仍无本轮通过证据。未执行微信体验版切换、正式小程序发布或生产 CloudBase 部署。

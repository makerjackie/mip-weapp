# 43 步原型与实现验收

2026-09-26，以流程画布 `https://airdrop.z-h-ai.com/p/715a5d/` 的 `cb5956b` 为对照版本，43 个流程步骤、40 个场景逐项记录。每步都有原型截图和真实微信开发者工具截图；没有用 `setData` 伪装身份、签到、会员到期或内容状态。

## 验收结果

[逐步记录与图片摘要](summary.json) 包含 43 项。当前结论：15 项同角色同状态视觉通过，2 项保留并说明配置/用户偏好差异，25 项实际账号或业务数据不匹配，1 项因未签到无法进入反馈表单。43 张实际截图并不代表 43 个业务场景全部通过，也不是物理手机验收。

完整单文件 HTML 位于本机 `.tmp/mip-parity-43-20260926.html`，可从 `http://127.0.0.1:18092/mip-parity-43-20260926.html` 查看。左原型、右实际，保持原图比例；长页附中部/底部截图。支持上一页、下一页、方向键、原型编号 hash、截图放大、逐页人工意见与 JSON 导出。全部 43 项导航和截图加载已经实际遍历；人工意见保持未填写。

## 本轮修复

- 账号设置统一留白与分组，只保留标题；退出登录事件绑定到实际按钮，退出确认/取消、退出后的游客态、重新微信登录均真实验证。注销仅测试展开和取消，未注销账号。
- 玩家等级背景保持素材比例；等级线、权益、任务与底部单层按钮重新对齐。经验明细真实展开/收起，“去完成”成功进入任务详情。
- 绑定手机号从本人资料读取脱敏号码，修复已绑定却显示未绑定；隐私开关尺寸、档案头部、人物卡、机会详情和案例图标修正。
- 原型的机会说明为选填，客户端与 API 已一致；已下架机会不再请求或展示评论，分享按钮正确禁用。
- 移除重复按钮包装组件与错误裁切图标，复用已有按钮/图标变种。
- 实际后台独立内容表单发现提交问题后，统一既有数据转换；真实创建并发布了明确标注演示的合作卡、案例和机会。未改真人会员资格、未发奖励。

## 后台与部署

4 项 Demo 权益、4 个 Demo 成长任务及经验/贡献奖励、用户协议和会员协议已写入后台配置并回读，小程序能读取。管理员可以后续修改。协议使用明确演示标识，不能作为正式条款。

后端 `mip-identity-api`、`mip-admin-api`、`mip-opportunities-api` 与后台 `2cb32a54` 均已部署，详见 [配置与发布证据](../demo-membership-20260926/README.md)。演示机会在测试已下架状态截图后恢复发布，最终状态为 `PUBLISHED`。

## 机会保存的明确阻塞

真实小程序编辑 Demo 机会、清空选填介绍并保存，已确认返回微信 `-604101`：该函数没有调用内容检查接口的权限。与本地选填校验无关，后台内容创建正常。已补充脱敏错误码诊断和保存失败即时提示；保持审查失败时拒绝保存。云函数最新部署健康，但微信权限尚未同步，不能记为保存通过。

本地 `config.json` 已声明唯一所需的 `security.msgSecCheck`。现有 MCP 通道更新 SCF 代码，不同步微信侧声明；开发者工具提供重新部署入口。因 `AGENTS.md` 规定云部署唯一通道为 `config/mcporter.json`，已请求用户允许这一次微信侧同步，批准前不执行备用部署。详见 [真实保存响应](opportunity-save-runtime.json)。

## 检查与剩余范围

- 完整 `pnpm verify:all`、`pnpm runtime:preflight` 和 `git diff --check` 已通过。Web 验证包含 150 项合同测试与 71 项 React 测试。
- 普通用户、新账号、签到嘉宾、临期玩家仍需相应真实测试身份；邀请/互动/心动非空状态仍需测试数据。不清空或变更当前真实玩家的资格来凑截图。
- 手机号授权/短信、支付、扫码签到、实际分享发送、长按删除与隐私偏好保存回读尚未全部验收；报告逐项列明。
- 预览二维码为临时开发预览，不据此声称已设置为微信公众平台长期体验版或正式发布。

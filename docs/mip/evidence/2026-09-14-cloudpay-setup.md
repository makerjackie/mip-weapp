# 2026-09-14 CloudPay 接入记录

## 范围与授权

- 用户确认商户与当前 MIP 小程序已绑定，JSAPI 和退款授权均已完成。本次未重复发起这两项授权。
- 沿用原生 `cloud.cloudPay`，不切换集成中心，不要求 APIv3 密钥。商户号仅写入本地配置和目标函数配置。
- 目标环境回读为 staging，会员目录为 TEST。用户指定会员、付费活动联调价为 0.10 元（10 分），真实付款和退款仍需手机验收。

## 已执行

- 管理 API Key 与环境绑定检查 READY。
- 唯一 ACTIVE TEST 会员方案 `annual_test` 原价 600000 分；通过精确 AppID、方案 ID、目录、原价与版本条件更新为 10 分，并回读确认。未改变历史订单金额或会员权益。
- 当前唯一付费活动为「MIP 早会（2030 年 12 月·主题场）」，回读价格仍为 39900 分；其改价等待运营后台登录，通过已有活动管理接口执行。
- 修复支付部署脚本的 `delay` 初始化顺序：首次等待云函数 Active 时原先抛 `ReferenceError: Cannot access 'delay' before initialization`，改为可提升的函数声明。
- `mip-cloudpay-callback` 已创建并完成配置回读，但调用健康检查返回 `Cam authentication failed`。另行收敛并回读 callback 禁止客户端调用，其他函数安全规则保持不变。
- `mip-payment-ledger` 的 test 模式配置已部署并回读；随后的健康检查未通过，因此不能声明 ledger 完整部署验收通过。

## 首次尝试与恢复

- 首次管理 API Key 通道不能完成函数健康调用。本地凭证文件检查曾为 UNUSABLE；用户随后明确允许本次 Device Flow，授权工具实际返回 READY 并复用已有登录态，无需扫码。后续命令显式使用 `CLOUDBASE_AUTH_MODE=local`，不改日常 API Key 通道。
- 恢复后支付适配器、回调和退款 worker 均已部署并通过配置、健康、权限与无 timer 检查。ledger/events/commerce 三个核心函数已逐个部署并通过健康检查。本地 `MIP_PAYMENT_MODE=test`，TEST 商品目录保持不变。
- 运营后台等待小程序确认登录，活动价格尚未改变。
- 本轮第一遍联合检查：小程序与服务端通过；Web React 55/56 通过，列表搜索/详情/分页测试超过既有 5000ms 超时，联合命令失败。未调整断言或超时。第二遍在小程序测试阶段遇到本机 1 分钟负载约 168；通过输出文件确认本次进程后，仅停止该检查进程树，记为未完成。支付部署专项 3 项测试及脚本 ESLint 通过，不能替代联合门禁。
- 恢复后的完整 `pnpm verify:all` 退出 0：小程序 221 文件 / 1192 项、Web Node 117 项及 React 11 文件 / 56 项通过，服务端、构建、包体与响应式合同通过。`runtime:preflight` 通过，开发者工具登录与服务端口有效。
- 未进行真实下单、扣款或退款，尚无真机支付成功证据。
- 最终 `CLOUDBASE_AUTH_MODE=local` 云端总验收退出 0：schema、最小权限、核心函数健康、支付配置及受保护调用规则通过。
- 在全量门禁通过后，复用已登录开发者工具执行 `weapp preview`，退出 0 并生成手机预览二维码。二维码仅保存在仓库外；未替换体验版、未发布正式版本。

## 续接

完成运营后台登录后，通过现有活动管理接口将联调付费活动改为 10 分并回读。手机需从会员入口新建订单，历史订单金额不随目录调整改变；实际付款前核对收银台金额为 0.10 元。真实支付、查单、回调发放及退款仍待手机闭环验收。

## 支付结果与用户文案修复

- 用户反馈结果页长时间确认。只读核对最近一笔 10 分会员订单仍为 `PAYMENT_CREATED`，`paid_at` 为空，回调表没有入账记录；这不证明微信未扣款。
- 原生 CloudPay V2 支付通知没有查单字段 `tradeState`，原处理器因此拒绝通知。最终修复直接按原生通知的 `returnCode/resultCode=SUCCESS` 解析付款成功，保留平台调用限制、AppID/身份/订单/金额校验及 ledger 幂等；不把统一下单的接口成功当付款成功。依据：[CloudBase 原生回调示例](https://docs.cloudbase.net/practices/use-wechat-pay)。
- 对照[微信 queryOrder 字段](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/reference-sdk-api/open/pay/CloudPay.queryOrder.html)发现查单请求错误地使用下单的驼峰写法；改为官方下划线参数，退款提交/查询存在同类问题并一起修复。补齐原生退款查询编号字段的解析。
- 曾尝试在通知入口先权威查单。管理端 SCF 调用返回 `-501001 invalid wx openapi access_token`，因缺少微信云调用令牌无法据此查出交易；最终移除这一不必要的通知前置查单及临时诊断。没有伪造付款或直接修改订单状态，已请用户在原订单刷新验证。

- 结果页改为读取当前订单，方案名称使用已有缓存，缺失时显示通用订单名称，不等待整批订单和方案请求。查询异常停止自动重试，显示可刷新或查看订单的提示；正常待支付状态仍有有界轮询。
- 清理会员、订单、支付结果、活动、帮助、关于和机会设置等页面中的服务端、归因和模型来源等技术文案；保留付款、退款和报名的真实业务边界。
- Chrome 已登录后台并成功执行付费活动下架，数据库回读 `UNPUBLISHED`、版本 12、价格仍 39900 分。随后 Chrome 原生界面仅返回残留菜单且无法截图；已请用户恢复前台窗口。编辑价格及恢复发布尚未完成。

### 本轮验证结果

- 原生通知、支付查单请求、退款请求和原生退款查询专项：18/18 通过。
- 最终支付适配器、回调及退款 worker 代码更新返回成功；随后云端总验收通过 schema、最小权限、函数健康及受保护调用规则。
- 两次完整 `pnpm verify:all` 的小程序 1194 项、服务端和小程序构建通过。Web Node 117 项通过；React 最终为 52/56，4 项超时，联合门禁未通过。降低并发到 2 后仍超时，单独运行 core-pages 的 5 项也有 2 项超时；未放宽超时、未改后台业务代码。
- 因联合门禁未通过，本轮没有上传新预览、体验版或正式版；手机上的旧前端文案尚未替换。
- 最后只读回查，10 分会员订单仍为 `PAYMENT_CREATED`、`paid_at` 为空。请用户在真实小程序登录态刷新原订单，不能声称支付闭环通过。
- 付费活动仍为 `UNPUBLISHED`、39900 分；尚需恢复 Chrome 操作后改为 10 分并重新发布。

- 后续以 `VITEST_MAX_WORKERS=1` 完整重跑：小程序和服务端继续通过，Web React 55/56，仍有 1 项 core-pages 测试超过 5000ms。联合门禁仍失败，未上传手机新版。
- 开发者工具曾成功打开结果页并读回新提示，随后查询进入 pending；自动化连接和异步调用不稳定，未取得可核实的付款成功事实。备用后台仍需登录，当前本地小程序仅支持扫码 token，而线上后台展示 6 位登录码，未借此绕过认证或修改权限。
- 文案收尾后的页面专项 28/28 通过，`git diff --check` 通过。

## 查单签名跨服务修复

- 用户在新生成的开发预览版中仍遇到“暂时无法查询支付结果”。检查发现真实适配器签名包含 `getPayableOrder.forSync`，ledger 验签字段清单却遗漏该字段，每次主动查单均会在访问微信前被拒绝。`applyPaymentCallback.providerPaidAt` 也有相同遗漏，携带付款时间的通知及查单补偿会被拒绝。
- 新增真实支付适配器/回调客户端对接 ledger 验签器的合同测试：修改前 3/3 均以 `FORBIDDEN` 失败；补齐两个签名字段后 3/3 通过，并校验篡改或移除这些字段仍被拒绝。相关支付、回调和 ledger Node 测试 84/84 通过。
- 仅更新云端 `mip-payment-ledger` 代码，未修改商品、订单、权限或密钥。部署返回成功。云端使用真实适配器签名、虚构订单 ID 的只读查单探针返回 `ORDER_NOT_PAYABLE`，证明新字段已通过验签且不存在的订单仍被正确拒绝；没有伪造付款或写订单状态。
- 已告知用户无需更换开发预览二维码，可刷新原订单。真机付款状态仍以实际刷新结果为准。
- 部署后的云端总验收退出 0，schema、最小权限、函数健康与受保护调用规则均通过。只读回查发现最新两笔 10 分会员订单仍为 `PAYMENT_CREATED`、`paid_at` 为空；未将技术修复等同于付款成功。
- 本轮完整 `VITEST_MAX_WORKERS=1 pnpm verify:all` 最终退出 0：小程序 222 文件 / 1197 项测试、服务端、构建与文档检查通过，Web lint/类型/测试/构建/响应式检查全部通过。新增合同测试的格式问题已修正后完整重跑；没有修改后台测试超时或业务断言。未上传体验版或发布正式版。

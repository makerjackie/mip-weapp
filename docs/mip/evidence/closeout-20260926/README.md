# 2026-09-26 收尾实现与验收

状态：代码已提交，云端发布等待维护者恢复登录。不是完整上线验收通过。

## 已完成实现

- 任务由服务端按待完成/已结束过滤后分页，游标绑定筛选条件，切换时丢弃过时响应。
- 机会独立记录本人合作表态：取消/重新表态、人数/头像/只读名单、发布人通知、个人“我想合作”列表；不迁移旧第三人引荐事实，不擅自增加奖励。
- Web 名片管理接真实档案，现有四套模板支持名称、排序、必填项和启停，用户名片可下架/恢复并查看版本历史；小程序生成名片受同一服务端配置约束。
- 修复现场活动名单真实 DTO 不兼容、排行榜快速切换竞态、机会页窄屏状态按钮换行挤压。
- 数据库权限收敛从全部撤销再重授改为只调整差异；150 张业务表权限已精确回读。
- 后台图片策略允许 HTTPS 图片，以支持云端头像。用户明确暂缓最终会员协议、正式等级权益和任务奖励表。

## 数据与发布事实

- 迁移 092/093 已应用，现有迁移共 94 个。执行前完成仓库外完整备份，148 张表 / 1646 行，按表回读行数。
- 飞书 506 个来源标签已导入并回读（486 个新增，20 个保留同名 ID），另导入 11 个徽章；不自动发放徽章，不删除旧引用。
- 实现提交 `e614979f3dbf06d5118e7f3a141aafe666c74bf0` 已推送并核对远端 SHA。
- outbox 代码更新步骤已执行，但健康调用返回 `[Invoke] Cam authentication failed`；重启 MCP 后仍失败。本机旧 Device Flow 不可用，恢复登录页面已打开。不能记为发布完成。
- identity/tasks/opportunities/admin 四个云函数、Web 新版和小程序新版上传尚未执行。为避免新界面依赖尚未发布的后端，保持现有网站部署。

## 自动检查

- `pnpm verify:all` 通过：244 个小程序测试文件 / 1334 项测试；服务端 656 个源码、278 个测试文件、47 种 outbox 事件合同通过；后台 146 项合同测试、63 项 React 测试通过；类型、lint、构建、预算、文档和响应式源码检查通过。
- `runtime:preflight` 通过，开发者工具已登录且服务端口开启。
- 部署等待期间，对本地新前端与现有云端遍历了 70 个路由：62 个进入接受态，8 个未通过，编译诊断 0。六个失败来自公开标签 UUID 数字串被误判手机号，扫描器已修复，8 项针对性隐私测试通过；保留旧报告，不把修复推断为重跑通过。
- 另两项：名片页面依赖新 identity action 尚未部署；对 TA 感兴趣名单返回受限，尚需核对样本公开性与当前身份，并补一个有权查看的公开样本。未放宽隐私权限，也未把 blocked 改成成功。
- loading/empty/error/forbidden/conflict/disabled 六种代表状态通过；严格自动交互尚未执行。路由接受态不等于每个按钮、每种身份或视觉还原率通过。

## 人工 UI 检查

通过真实点击与滚动，而非注入页面处理函数：

- 小程序首页加载失败后的重试恢复非空活动；个人页、等级页、经验值详情、团队 PK 能进入并显示真实测试数据。
- 新构建的机会页城市目录可展开；选择上海后只显示上海机会，清除筛选后恢复列表。375px 状态按钮修正后不再折行，已实拍。
- 新构建等级页显示 Lv.1、50/500、正式权益未配置；进入任务列表显示“完善资料”。任务服务端新筛选尚未部署，切换结束页签仍使用旧云端结果，不能算新版任务筛选验收通过。
- 活动现场名单的新适配在路由实拍中显示非空名单；未执行真实签到/撤销。
- Web 使用小程序确认网页登录成功，网站概览显示真实数据，用户列表和“林夏”筛选返回非空结果。
- Web 对受控演示用户创建不含手机号的导出，文件下载阶段显示“导出文件下载失败”。这是未解决问题，未宣称导出成功。

本地截图与原始日志保留在 `.tmp/closeout-20260926/`、`.tmp/runtime-evidence/closeout-20260926/`；可查看本地 `mip-closeout-20260926.html`。截图不包含账户凭证或导出文件。旧原型 HTML 继续保留为历史对照，不能代替新版验收。

## 接续工作

1. 恢复腾讯云部署登录后，完成五个函数部署与完整 cloud:verify，再部署 Web 生产分支 main，核对正式域名资源与真实接口。
2. 复测合作的表态/取消/名单/通知及个人列表；任务分类分页；名片模板、下架/恢复和历史；定位并修复 Web 导出下载失败。
3. 重跑 70 路由与严格交互，补公开感兴趣名单样本；Web 1440×900、1280×720、390×844 的新版实际界面与操作验收仍待完成。
4. 全部门禁通过后上传小程序新开发版。手机号、支付、通知送达、扫码和相册仍需真机。

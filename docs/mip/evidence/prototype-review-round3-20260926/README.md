# 43 步验收第三轮修复（2026-09-26）

本轮继续处理第二轮真实失败及可独立补测项。当前复核范围内没有仍未解决的已确认代码/UI 缺陷；这不表示 43 步所有角色和交互均通过。截图核对、交互记录、角色/数据覆盖、真机能力已分开显示，不再把缺少目标账号混作代码故障。

## 修复

- **机会无法保存**：云端缺少已在源码声明的 `security.msgSecCheck` 权限，实际调用报 `-604101`。用户明确允许一次仅通过已登录微信开发者工具重部署 `mip-opportunities-api`，同步已有权限；没有绕过内容安全检查。22:47:52 部署完成，标准通道回读 Active/Available，16 组运行配置保持一致。
- **活动反馈保存后不返回**：保存成功返回同一活动详情；历史栈不是该活动时使用明确详情地址，返回失败也有回退。冲突或保存失败仍留在表单。底栏复用现有圆胶囊组件，移除全宽矩形外框。
- **公开设置覆盖隐私偏好**：保存展示字段时保留既有隐藏字段，避免把“人才搜索隐藏”等偏好重置。成功回读使用合同中的 `phoneBound`，不再把已绑定手机号误显示成未绑定。
- **本人内容删除的竞态**：在确认前加操作锁；取消及原生异常释放；忽略删除前发起的旧列表响应；删除成功但刷新失败时不再显示已删除卡片。覆盖本人档案、公开档案及独立内容列表。
- **验收页状态混淆**：视觉、交互、角色/数据和真机分项展示；真实交互失败不会被视觉通过遮盖。更换截图使旧通过失效，用户历史批注保持只读。

## 实际操作证据

| 流程 | 本轮结果 | 边界 |
| --- | --- | --- |
| 编辑已发布演示机会 | 原生点击保存，版本 6 → 7；重进详情回读新发布时间 | 未验证微信分享接收端或真实删除 |
| 新增机会 | 必填内容明确写演示、介绍留空；原生点击发布后返回列表；重进编辑为 PUBLISHED/version 1，介绍仍为空 | 表单值通过真实页面输入处理器填写，不代替物理键盘/剪贴板验收 |
| 已签到活动反馈 | 找到既有历史到场演示活动；实际入口 → 保存 → 返回同一详情 → 重进回读版本 3，答案保留 | 未新增签到、没有自动同意协议 |
| 感兴趣及名单 | 演示档案实际点击感兴趣，0 → 1；打开非空名单、点人进入档案；返回取消恢复 0 | 返回一条、无下一页游标；不外推多页或举报提交 |
| 隐私跨页保存 | 开启隐藏 → 保存公开设置 → 重进仍隐藏；随后恢复原来两项 false 并回读 | 没有遗留测试隐私设置；手机号状态保持已绑定 |
| 三类本人内容取消删除 | 页面删除入口处理器打开原生确认框，实际点取消；卡片和数量保留、锁释放 | 未执行永久删除，不声称物理长按通过；竞态由行为测试覆盖 |

## 验证与交付范围

- 最终 `pnpm verify:all` 通过：小程序 253 个测试文件、1449 项测试；Web 71 项 React 测试、150 项合同测试；类型、构建、预算及仓库其余检查通过。
- `pnpm runtime:preflight` 通过，真实开发者工具登录及服务端口可用。预检不替代业务或真机验收。
- 43 组原型/实际主图齐全；新增相关场景实拍，未变更图保留第二轮复核记录与原采集时间。浏览器实际逐页点击 43 步，120 张主图/补图均加载，最后一步禁用下一步。批注、存储隔离与导出由 11 项生成器行为测试覆盖。
- 当前记录：已知问题 0；角色/数据待补 24；AI 尚有测试分支 35；仅剩专项真机项 1。分类重叠，**不是 35 个代码缺陷，也不是其余 42 页都无需真机**；完整范围按各页记录读取。
- 新 HTML 与原始截图、真实账号数据、旧用户意见只存本地 `.tmp/`，不提交到仓库。演示新增机会有明确虚构说明，可在后台继续编辑。
- 本轮只更新上述单个云函数权限；未重新部署无变更的 Web，未上传新的小程序体验版。Git、本机构建、云端权限和微信版本分别判断。

普通非玩家、已签到非玩家、新账号及临期会员的完整门禁仍需各自真实测试账号；原生取号、短信换绑、扫码及支付仍需手机。AI 可自行完成的其余测试分支继续归 AI，不统一交给用户。没有改当前真实会员期限、捏造账号身份或用空态替代非空验收。

本记录取代[第二轮](../prototype-review-round2-20260926/README.md)中“内容保存仍受权限限制”的当前结论。历史失败记录保留作溯源。

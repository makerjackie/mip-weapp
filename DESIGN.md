# DESIGN

MIP 的界面以 [Figma 页面映射](docs/mip/FIGMA_MAP.md) 为视觉依据，以本文件的 token、组件和状态规则约束实现。Figma 只作为设计输入，不把整张原型图打包进生产页面。

## 设计方向

界面采用黑色底、深灰面板、MIP 黄选中态和白色主文本。信息优先级清楚，操作直接，不增加口号、概念说明或内部开发文案。

## 品牌资产

- 主图标：黄色背景、黑色 MIP 标志。
- 透明黑色标志：仅用于浅色或黄色背景，使用前检查对比度。
- 品牌入口：`src/config/brand.ts`。
- 页面禁止自行硬编码品牌色或从 Figma 截图裁切 Logo。

## 色彩 Token

| Token       | 值        | 用途                     |
| ----------- | --------- | ------------------------ |
| canvas      | `#040404` | 页面背景                 |
| panel       | `#202020` | 卡片、表单和 TabBar      |
| panelRaised | `#2A2A2A` | 浮层和选中前景           |
| ink         | `#FFFFFF` | 主文本                   |
| muted       | `#A3A3A3` | 次级文本                 |
| line        | `#363636` | 分隔线和边框             |
| brand       | `#FCDF03` | 主按钮、选中态、重要图标 |
| onBrand     | `#040000` | 黄色背景上的文字和图标   |
| brandActive | `#E3C900` | 按压态                   |
| brandSoft   | `#3B3505` | 黄色弱强调背景           |
| danger      | `#E65C5C` | 失败和危险操作           |
| success     | `#43B581` | 成功状态                 |

`#FCDF03` 和 `#040000` 来自 MIP Logo。screen-specific 色值在实现目标页面时通过 Figma design context 核对；不得为了“更有品牌感”自行增加渐变或高饱和辅助色。

## 字体与字号

使用微信稳定字体：`PingFang SC`、`Hiragino Sans GB`、`Microsoft YaHei`。主标题 `36–40rpx`，区块标题 `30–34rpx`，正文 `26–28rpx`，辅助信息 `22–24rpx`。

标题用 semibold/bold，正文 regular。正文不低于 `24rpx`；次级文字仍需满足深色背景对比度。

## 间距、圆角和阴影

- 基准间距 `8rpx`；页面水平边距 `24rpx`。
- 同组间距 `12–24rpx`，区块间距 `32–48rpx`。
- 卡片圆角 `16rpx`，输入和按钮圆角 `12–16rpx`，标签圆角按胶囊形处理。
- 普通卡片使用色块层级或 `1rpx` 边框，不使用大面积投影。
- 卡片封面比例固定；文字长度变化时不能挤压状态和主操作。

## 主导航

四个主 Tab 固定为：发现、活动、机会、我的。图标在上、文字在下；选中态使用品牌黄，未选中态使用 muted。

Tab 根页不显示返回按钮。管理入口放在“我的”，通过服务端 capability 决定可见与可用状态。

微信自定义 TabBar 必须：

- 由 `onShow` 同步当前索引；
- 使用 `t-icon`，`app.json` 栅格图只作微信回退资源；
- 在组件自身 wxss 中提供不透明 panel 底色和品牌 token；
- 保留 `safe-area-inset-bottom`，避免页面内容透出或遮挡。

## 导航与退出

主 Tab 使用原生导航栏或 Figma 已定义的安全自定义结构。二级页保持侧滑返回，并在页面栈为 1 时提供明确的“返回”“完成”或“返回首页”。不得用 `reLaunch` 把用户留在无法退出的二级页。

登录、协议和资料补全属于可恢复流程：完成或取消后回到来源页，恢复原业务动作；不要固定跳回首页。

## 页面和组件

- 活动卡：封面、时间、地点、状态、参与人数和活动限制都有文字信息。
- 机会卡：封面、标题、价值、城市、目标和引荐人数按 Figma 层级展示。
- 合作卡：角色、目标、合作需要和雷达图使用统一数据合同；角色名称来自配置。
- 表单：标签在控件上方，必填、字数、错误和禁用状态明确。
- 主按钮：TDesign large，高度 `88–92rpx`；黄色背景使用 onBrand 文本。
- 列表：使用 `flex flex-col gap-*`，不使用 `space-y-*`。
- 图标：只使用已注册的 TDesign 图标或正式品牌 SVG，不用字符模拟图标。

## 状态

- Loading：列表和详情使用稳定骨架，不用一个 spinner 撑满全页。
- Empty：使用 `app-empty-state`，说明事实并给出当前可执行操作。
- Error：保留上下文，显示可理解的错误和重试。
- Forbidden：说明当前没有权限，不泄露所需角色或敏感信息。
- Conflict：状态已变化时刷新服务端事实并引导用户重试。
- Disabled：缺少支付、消息或 AI 配置时禁用对应动作，文案描述当前状态。

支付中、待确认、成功、失败必须分别呈现；客户端完成 `requestPayment` 只能进入待确认，不能直接显示权益已生效。

## 反馈与动效

成功使用 toast 和页面状态同步反馈；失败保留已填写内容并说明下一步。动效只用于状态切换和轻触反馈，不增加循环装饰动画。

## 可访问性

- 点击热区至少 `88rpx`。
- 状态不能只靠颜色表达。
- 表单标签、错误和按钮有明确文字。
- 深色背景上的正文和操作保持足够对比度。
- 安全区、长文本、系统字体放大和空数据状态均需运行时检查。

## 文案

采用朴素、中性、专业的表达。标题使用行业通用名称，例如“活动”“机会”“我的订单”“玩家档案”。不写口号、反问、比喻、营销腔或内部实现说明。

六种合作角色是已确认的业务展示名，可按正式审核要求通过配置整体替换；代码、权限和数据迁移只使用稳定角色 key。

## 新页面验收

1. 目标 frame 已通过 Figma design context 和截图核对。
2. 色彩、字号、间距和圆角来自 token。
3. 有 loading、empty、error、forbidden、conflict 和 disabled 中适用的状态。
4. 底部安全区和二级页退出路径可用。
5. 页面不直接调用 CloudBase、支付或外部通知接口。
6. 原型截图和临时素材未进入生产资源。
7. 375px 代表机型和长内容下无裁切、重叠和不可达操作。
